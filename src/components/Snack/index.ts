import { Accept, AcceptProps } from './components/Accept';
import { Reject, RejectProps } from './components/Reject';

export type {
  AcceptProps as SnackAcceptProps,
  RejectProps as SnackRejectProps,
};

export const Snack = {
  Accept,
  Reject,
};
